{
  "name": "frost-ai-roadmap",
  "version": "1.0.0",
  "description": "Frost.ai Roadmap - Team 28",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "deploy": "npm run build && git add -A && git commit -m 'Deploy to GitHub Pages' && git push origin main",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
